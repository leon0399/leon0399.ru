name: Test

on:
  pull_request:
  push:
    branches: [master]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16]
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          cache: "yarn"
          node-version: ${{ matrix.node-version }}

      - name: Install Dependencies
        run: yarn --prefer-offline --frozen-lockfile --ignore-engines --ignore-optional --non-interactive --silent--ignore-scripts=false --foreground-scripts --production=false --platform=linux --arch=x64
        env:
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: true
          HUSKY_SKIP_INSTALL: true

      - name: Lint code
        run: yarn lint

      - name: Lint types
        run: yarn lint:types
